set-option -g status-position top

# better colors
set-option -sa terminal-overrides ",xterm*:Tc"

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# shift alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# set -g @catppuccin_flavour 'mocha'

# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
# set -g @plugin 'tmux-plugins/tmux-yank'

# run '~/.tmux/plugins/tpm/tpm'

# set vi-mode
set-window-option -g mode-keys vi
# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# open split pane with current path
bind 'h' split-window -v -c "#{pane_current_path}"
bind 'v' split-window -h -c "#{pane_current_path}"
unbind '%'
unbind '"'


# tmuxplugin-catppuccin
# ---------------------
# set -g @catppuccin_flavor "latte"

# set -g @catppuccin_status_background "none"
run-shell '#{d:current_file}/plugins/pm4iz4gr0is27ig6awk9arl1cfybchw8-tmuxplugin-catppuccin-unstable-2024-05-15/share/tmux-plugins/catppuccin/catppuccin.tmux'
# set -g status-right-length 100
# set -g status-left-length 100
# set -g status-right '#[fg=#{@thm_crust},bg=#{@thm_teal}] tf: #S '
# set -g status-left ""
# set -g status-right "#{E:@catppuccin_status_application}"
# set -agF status-right "#{E:@catppuccin_status_cpu}"
# set -ag status-right "#{E:@catppuccin_status_session}"
# set -ag status-right "#{E:@catppuccin_status_uptime}"
# set -agF status-right "#{E:@catppuccin_status_battery}"

# tmuxplugin-yank
# ---------------------
run-shell '#{d:current_file}/plugins/5z9v880jj96c2vqs6v6r12x7wa7hgqn2-tmuxplugin-yank-unstable-2023-07-19/share/tmux-plugins/yank/yank.tmux'

# tmuxplugin-vim-tmux-navigator
# ---------------------
run-shell '#{d:current_file}/plugins/mrphrg7g4mr2zicsv31y5p8mm8b1bdsa-tmuxplugin-vim-tmux-navigator-unstable-2022-08-21/share/tmux-plugins/vim-tmux-navigator/vim-tmux-navigator.tmux'

# tmuxplugin-cpu
# ---------------------
run-shell '#{d:current_file}/plugins/dqaggcfcdhghc2hvynnprzh0dbsnl4wc-tmuxplugin-cpu-unstable-2023-01-06/share/tmux-plugins/cpu/cpu.tmux'

# tmuxplugin-battery
# ---------------------
run-shell '#{d:current_file}/plugins/xvb7iblih2g8rnnbfvjh8dmpyqc20bk1-tmuxplugin-battery-unstable-2019-07-04/share/tmux-plugins/battery/battery.tmux'

# ============================================= #

